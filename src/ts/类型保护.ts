function handel(idCard: string | number) {
  if (typeof idCard === "string") {
    idCard;
  } else {
    idCard;
  }
}

interface Person {
  name: string;
  age: number;
}

let k: keyof Person;
k = "age";
console.log(k);
